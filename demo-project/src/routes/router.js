import { createRouter, createWebHistory } from "vue-router"
//import Tablas from "@/views/Tablas.vue";

const routes = [
    //{ path: '/', name: Tablas, component: Tablas }
]

const router = createRouter({
    history: createWebHistory(),
    routes

})

export default router